@model List<Course>





@* <div class="typing-container">
    <h1 class="typing-text">Hoş Geldiniz!  </h1>
</div>
<p>  🚀 Kendinizi geliştirmek için en doğru adrestesiniz. </p> *@

<div class="welcome-header">
    <h1 class="typing-title">Hoş Geldiniz!</h1>
    <p class="welcome-subtext">
        🚀 Kendinizi geliştirmek için en doğru adrestesiniz.
    </p>
</div>


<h2 class="page-title">📚 Tüm Kurslar</h2>

<!--  FİLTRE ALANI -->
<form method="get" class="search-bar mb-4">

    <input type="text"
           name="search"
           value="@ViewBag.Search"
           class="search-input"
           placeholder="Kurs ara..." />

    <select name="category" class="search-input">
        <option value="">Tüm Kategoriler</option>
        @foreach (var cat in ViewBag.Categories as List<string>)
        {
            <option value="@cat"
                    selected="@(cat == ViewBag.SelectedCategory)">
                @cat
            </option>
        }
    </select>

    <select name="instructor" class="search-input">
        <option value="">Tüm Eğitmenler</option>
        @foreach (var inst in ViewBag.Instructors as List<string>)
        {
            <option value="@inst"
                    selected="@(inst == ViewBag.SelectedInstructor)">
                @inst
            </option>
        }
    </select>

    <button type="submit" class="search-btn">
        🔍 Filtrele
    </button>

</form>

@if (Model == null || !Model.Any())
{
    <p class="text-muted mt-4">Kurs bulunamadı.</p>
}
else
{
    <div class="row mt-4">
        @foreach (var course in Model)
        {
            <div class="col-md-4 mb-4">
                <div class="course-card">

                    <img src="https://picsum.photos/600/400?random=@course.Id"
                         class="course-img" />

                    <div class="card-body">
                        <h5 class="card-title">@course.Title</h5>

                        <p class="course-description">
                            @course.Description
                        </p>

                        <p class="course-category">
                            @course.Category
                        </p>

                        <p class="course-instructor">
                            👨‍🏫 @course.Instructor?.User?.UserName
                        </p>

                        <a asp-area="Identity"
                           asp-page="/Account/Login"
                           class="btn btn-primary btn-sm w-100 mt-2">
                            Giriş Yap & Katıl
                        </a>
                    </div>

                </div>
            </div>
        }
    </div>

}

<footer class="main-footer">
    <div class="container">
        <div class="row">
            <div class="col-lg-4 mb-4 mb-lg-0">
                <h5 class="footer-title">🎓 KursAkademi</h5>
                <p>Geleceğin teknolojilerini bugünden öğrenin. Uzman eğitmen kadromuzla kariyerinizde yeni bir sayfa açın.</p>
               
            </div>
     
            <div class="col-lg-3 col-md-6 mb-4 mb-lg-0">
                <h5 class="footer-title">Popüler Kategoriler</h5>
                <ul class="footer-links">
                    <li><a href="#">Web Geliştirme</a></li>
                    <li><a href="#">Veri Bilimi</a></li>
                    <li><a href="#">Mobil Uygulama</a></li>
                    <li><a href="#">Grafik Tasarım</a></li>
                </ul>
            </div>

            <div class="col-lg-3">
                <h5 class="footer-title">İletişim</h5>
                <ul class="footer-links">
                    <li><i class="fas fa-map-marker-alt me-2"></i> İstanbul, Türkiye</li>
                    <li><i class="fas fa-envelope me-2"></i> info@kursakademi.com</li>
                    <li><i class="fas fa-phone me-2"></i> +90 212 123 45 67</li>
                </ul>
            </div>
        </div>

        <div class="footer-bottom text-center">
            <p>&copy; 2025 KursAkademi. Tüm Hakları Saklıdır. 🚀</p>
        </div>
    </div>
</footer>